# Git hooks manager configuration
# See https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    lint:
      run: npm run lint
    format:
      run: npm run format --check
