# Git hooks manager configuration
# See https://github.com/evilmartians/lefthook

pre-commit:
  parallel: true
  commands:
    lint:
      run: pnpm run lint
    format:
      run: pnpm run format --check
